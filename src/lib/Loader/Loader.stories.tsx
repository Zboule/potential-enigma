import React from 'react'
import { MyLoader } from './Loader.component'

export default {
    title: 'Loader',
    component: MyLoader,
}

export const Default = () => {
    return (
        <MyLoader />
    )
}
